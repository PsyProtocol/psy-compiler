#[derive(Storage)]
pub struct Person {
    pub age: Felt,
}

#[contract]
#[derive(Storage)]
pub struct SimpleContract {
    #[RefType]
    pub b: Person,
}

fn main() {
    let c = SimpleContractRef::new(ContractMetadata::current());
    c.b.age = 11;
    assert_eq(c.b.age, 11, "c.b.age == 11");
}