fn add(a: Felt, b: Felt) -> Felt {
    let mut c: Felt = a + b;
    return c;
}

fn sub(a: Felt, b: Felt) -> Felt {
    let mut c: Felt = a - b;
    return c;
}

fn mul(a: Felt, b: Felt) -> Felt {
    let mut c: Felt = a * b;
    return c;
}

fn add_one(i: Felt) -> Felt {
    let mut c: Felt = i + 1;
    return c;
}

fn fun(x: [Felt; 2]) {
}

// input:
// output:
fn main() {
    let mut a: Felt = add(1, 2);
    let mut b: Felt = sub(2, 1);
    let mut c: Felt = mul(2, 2);
    let mut e: Felt = sub(2, 1) + mul(2, 2);
    let mut d: Felt = add(1 + 2, 2);
    let mut f: Felt = add(add(1, 2), 2);
    let mut g: Felt = add(1 + 3, add(3 + 5, 8 - mul(add(sub(10, 5), 5), 2)));
    let mut h: Felt = add_one(1);
    let mut i: Felt = add_one(add_one(add_one(add_one(add_one(add_one(add_one(add_one(add_one(1 + add_one(1 + add_one(1 + add_one(1 + 2)))) + 2) + 2) + 2))))));
    // Expected pub Array (TypeId(432)), but found pub Array (TypeId(499))
    // let x: [Felt; 3] = [1, 2, 3];
    // fun(x);
}