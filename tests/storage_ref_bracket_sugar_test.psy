#[contract]
#[derive(StorageRef, Storage)]
pub struct C {
    pub arr: [Felt; 2],
}

fn main() {
    let c = CRef::new(ContractMetadata::current());
    c.arr[0] = 11;
    c.arr[1] = 22;
    c.arr[0] += 3;
    assert_eq(c.arr[0].get(), 14, "arr[0]");
    assert_eq(c.arr[1].get(), 22, "arr[1]");
}